<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"/>
    <link rel="shortcut icon" href="data:image" type="image/x-icon">
    <link rel="stylesheet" href="/css/style.css">
    <title>My Secret Santa</title>
    <script src="https://kit.fontawesome.com/b3d05658f1.js" crossorigin="anonymous"></script>
    <% if (locals.user) {  %>
        <script defer src="/js/index.js"></script>
    <% } %>
    <% if (locals.draw) { %>
        <script defer src="/js/draw.js"></script>
    <% } %>
</head>
<body class="flex flex-col text-font h-screen">
    <header class="w-full bg-red-500 flex max-sm:flex-col h-32 max-sm:justify-around justify-between items-center px-4">
        <div>
        <h1 class="title-font text-2xl md:text-3xl lg:text-5xl text-lime-300 hover:cursor-pointer"><a href="/">My Secret Santa</a></h1>
        </div>
        <nav class="">
            <ul class="h-full flex justify-around text-sm md:text-xl lg:text-2xl text-red-500">
                <% if (locals.user) { %>
                    <li class="bg-lime-300 p-1 pt-0.5 md:p-3 md:pt-1 mx-2 my-2 rounded-2xl hover"><a href="#" class="">Mon MySecretSanta</a></li>
                    <li class="bg-lime-300 p-1 pt-0.5 md:p-3 md:pt-1 my-2 rounded-2xl hover"><a href="/logout">Se déconnecter</a></li>
                <% } else { %>
                    <li class="bg-lime-300 p-1 pt-0.5 md:p-3 md:pt-1 mx-2 my-2 rounded-2xl hover"><a href="/register" class="">S'inscrire</a></li>
                    <li class="bg-lime-300 p-1 pt-0.5 md:p-3 md:pt-1 my-2 rounded-2xl hover"><a href="/login">Se connecter</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main class="h-full flex justify-center p-3 overflow-scroll">