<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="shortcut icon" href="data:image" type="image/x-icon">
    <link rel="stylesheet" href="/css/style.css">
    <title>My Secret Santa</title>
    <script src="https://kit.fontawesome.com/b3d05658f1.js" crossorigin="anonymous"></script>
    <% if (locals.user) {  %>
        <script defer src="/js/index.js"></script>
    <% } %>
    <% if (locals.draw) { %>
        <script defer src="/js/draw.js"></script>
    <% } %>
</head>
<body class="flex flex-col text-font h-screen">
    <header class="w-full bg-red-500 flex h-32 justify-between items-center px-4">
        <div>
        <h1 class="title-font text-5xl text-lime-300 hover:cursor-pointer"><a href="/">My Secret Santa</a></h1>
        <!-- <span>Votre tirage au sort complètement aléatoire, avec liste de souhaits, pour garantir un Secret Santa réussi.</span> -->
        </div>
        <nav class="w-1/4">
            <ul class="flex justify-around text-2xl text-red-500">
                <% if (locals.user) { %>
                    <li class="bg-lime-300 p-3 pt-1 rounded-2xl hover:cursor-pointer hover:font-bold hover:scale-105"><a href="#" class="">Mon MySecretSanta</a></li>
                    <li class="bg-lime-300 p-3 pt-1 rounded-2xl hover:cursor-pointer hover:font-bold hover:scale-105"><a href="/logout">Se déconnecter</a></li>
                <% } else { %>
                    <li class="bg-lime-300 p-3 pt-1 rounded-2xl hover:cursor-pointer hover:font-bold hover:scale-105"><a href="/register" class="">S'inscrire</a></li>
                    <li class="bg-lime-300 p-3 pt-1 rounded-2xl hover:cursor-pointer hover:font-bold hover:scale-105"><a href="/login">Se connecter</a></li>
                <% } %>
            </ul>
        </nav>
    </header>
    <main class="h-full flex justify-center p-3">